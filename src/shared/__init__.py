"""The contract shared by the pipeline and the API (DESIGN.md > What this is): schema, config, run record,
the model client with record/replay, and the three brief queries. Neither part imports the other."""
